page: test
    module: fortran

    real dist, vel
    dist = 15.68323
    vel  = 0.0270043
    $print " DISTANCE = %7.4f", dist
    $if vel <1
        $print " VELOCITY = %7.4f", vel

    i=1
    $while i<10
        $print " i = %d", i
        i=i+1

    $print " F(4) = %7.4f", F(4.0)

fncode: F(f_x)
    $return_type REAL

    $if f_x<0
        F=f_x*f_x
    $else
        F=f_x+2.0

